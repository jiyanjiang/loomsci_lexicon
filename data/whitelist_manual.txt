# 用户自定义白名单（手动维护，pipeline2 操作位）
#
# 用途：找回被消杀/未加载的概念（如 STOP 误杀、1-token 缺省不加载）。
# 与 LLM 白名单同级，在标注阶段加载；可视化时强制包含并金边标记。
#
# 规则：
#   - 每行一个词，最多 5 个（超过部分忽略）
#   - # 开头为注释
#   - 词会被统一 tokenize 规范化后加入标注词典
#
# 测试白名单（2026-08-08）：
Transmon
surface code
